{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit",
    "build:web": "npm run check && vite build",
    "build:wasm": "wasm-pack build ../core_engine --target web --out-dir ../web/pkg/wasm",
    "build": "npm run build:wasm && npm run build:web",
    "preview": "vite preview"
  },
  "keywords": [],
  "author": "M-Mrk",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "devDependencies": {
    "monaco-editor": "^0.53.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "chart.js": "^4.5.1"
  }
}
